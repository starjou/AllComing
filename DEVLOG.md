# AllComing 開發日誌

## 開發背景

這個專案是我的第一個 Cocos 遊戲專案，以 Claude 輔助，在兩天內從頭開始到完成。

我的主要技術背景是 PHP/Laravel 和 Vue.js。選擇做這個專案的原因是評估自己能否快速切入遊戲前端領域，並作為 portfolio 展示學習能力。

開發方式是：我主導所有設計決策和開發節奏，Claude 作為技術顧問提供 Cocos API 知識、撰寫程式碼和 debug。由我判斷方向、調整功能並下最終決定。

---

## 遊戲設計決策

遊戲的核心玩法是在打算用 Cocos 做遊戲時，腦中浮現的一種常見的 2D 砲塔防禦遊戲。與 Claude 討論後決定做成 3D 版本來跟原來的遊戲做出一點差異性。

設計過程中幾個關鍵決策：

**技能系統：** 原本只有四個技能：子彈頻率、速度、爆擊率、爆擊傷害倍率，意識到沒有傷害技能的話爆擊沒有意義，所以擴展成七個技能。

**升級費用公式：** 從線性成長改成指數成長，避免後期金錢用飛的情況。最終採用 `基礎費用 × 成長率^等級` 的公式，成長率作為可調參數。

**3D 視角：** 原本考慮 2D 俯視，最後選擇 3D 俯視加上可拖曳旋轉的相機，作為差異化的設計點。

**敵人難度曲線：** 以分數作為難度參數，分數越高敵人生成越快、血量越高、速度越快，所有係數都抽成可調參數方便平衡測試。

---

## 遭遇的主要技術問題

**1. `require` vs `import`**

Cocos 3.x 的 TypeScript 環境不支援 `require()`，初版程式碼全部用 `require` 導致 runtime error。解法是改用標準 ES module `import`，並把 GameManager 改成可 export 的單例。這算 Claude 掉的溝。

**2. Button 回調的參數問題**

Cocos Button 的 Click Event 回調函式簽名是 `(event, customEventData)`，第一個參數是事件物件，不是 customEventData。導致技能升級傳入的 index 變成 EventTouch 物件，計算出 NaN。這也是 Claude 掉的溝。我的角色比較像 QA 及企劃，負責回報及測試。

**3. 物理引擎移動失敗**

在初版完成時，覺得子彈的移動不太連貫，有殘影，跟 Claude 討論後，我本來以為是 `setWorldPosition` 跟對物體設定速度這兩種不同的移動物體的方式造成的，然後嚐試把 `setWorldPosition` 改成用 `RigiBody.linearVelocity`，但是不知道為什麼東西不會動。Claude 嘗試了時序問題、Allow Sleep、Load Manually、各種初始化順序都無效，我在網路上找到 Google AI 提供的這個問題的種種處理方式也都沒用。

最後決定放棄物理移動，改回 `setWorldPosition` 逐幀移動，碰撞改用距離偵測，先讓專案可能完成。我也了解 `setWorldPosition` 的殘影問題是在於子彈運動速度，而不是在於做法, 在 60fps 下視覺效果基本上還行吧。

**4. 碰撞偵測策略**

子彈和敵人的碰撞最初用 Cocos 的 Collider 碰撞事件，可以正常運作。敵人碰到砲塔的偵測則改用距離計算，因為 Collider 事件在這個情境下觸發不穩定。

---

## 技術架構

採用單例 GameManager 集中管理所有遊戲狀態，其他腳本透過 `GameManager.instance` 存取。UI 全部由 UIManager 在 `update` 裡每幀更新，排行榜用 localStorage 儲存。

---

## 學習心得

這兩天用 Claude 輔助建立了第一個 Cocos 遊戲的感想是：大概知道了 Cocos 遊戲開發的觀念，Creator 的介面、用法。Claude 包辦了絕大多數的 coding 及知識面，而我則扮演一個專案經理的角色，並在某些除錯的過程中防止 Claude 鑽牛角尖繞太遠。

兩天的時間其實並沒有時間去完整的看過程式碼，所以可以說給于 Claude 的程式很大的信任，只靠自行測試找出問題讓他修正，這其實是我比較不習慣的開發方式。但是在開發效率上是非常大的增進，我理想的模式是在功能開發完，還是要完整的消化完 AI 產出的程式碼，因此開發時間不能以程式寫完，測試完花的時間來計算，這個在之後實務工作再調整吧。
